"""Shared app support: constants, route helpers, session state. Used by app.py and routes.

Import from submodules:
  from core.constants import HTTP_BAD_REQUEST, ...
  from core.helpers import ensure_user_vis_dir, with_prefix, allowed_file, ...
  from core.state import get_session_id, get_session_storage, SESSION_COOKIE_NAME
"""
