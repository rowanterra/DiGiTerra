# Scripts

Standalone runnable scripts for DiGiTerra (one-off jobs, dev helpers, data prep).  
These are **not** imported by the app. For app and pipeline code, see `python_scripts/` and `app.py`.

Example usage:
- `python scripts/your_script.py`
