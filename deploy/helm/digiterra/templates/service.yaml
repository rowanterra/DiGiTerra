apiVersion: v1
kind: Service
metadata:
  name: {{ include "digiterra.fullname" . }}
  labels:
    {{- include "digiterra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digiterra.selectorLabels" . | nindent 4 }}
